---
phase: 06-coordinator-overview-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/app/(dashboard)/page.tsx
  - src/app/(dashboard)/demandas/demanda-list.tsx
autonomous: false
requirements: [COORD-01]
must_haves:
  truths:
    - "A coordenador_geral sees a 'Painel do coordenador' text link on the / (personal demandas list) page's existing header row, giving them one visible, obvious path into the new /painel dashboard — per 06-UI-SPEC.md's Entry Point contract (a single link, not a new nav bar, since this project has no persistent nav/header component yet and building one is out of this phase's scope)."
    - "Every role other than coordenador_geral never sees this link at all — the link's visibility is gated by the SAME server-side profiles.role read page.tsx already performs for the existing role-scoped-view notice (no new query, no client-side role check), consistent with this project's established 'hiding is UX only, RLS is what actually matters' framing."
    - "The link is styled as a secondary/outline action (border-zinc-400 bg-white text-zinc-900, matching the existing 'Limpar filtros' desktop treatment), min-h-14, placed next to the 'Nova demanda' primary button in demanda-list.tsx's existing header row, using the same flex-col sm:flex-row responsive stacking pattern that row already has — not a new layout primitive."
  prohibitions:
    - "MUST NOT add a persistent nav bar, header component, or any navigation element beyond the single locked text link — 06-UI-SPEC.md's Entry Point contract explicitly scopes this to one link, framing a full nav redesign as out of this phase's scope."
    - "MUST NOT compute the coordenador_geral check via a second, separate query — reuse the `role` value page.tsx's existing profile read already fetches for the role-scoped-view notice; this plan threads that same value down to demanda-list.tsx as a new prop, it does not re-fetch it."
    - "MUST NOT use the blue accent color for this link — 06-UI-SPEC.md reserves blue-700 for 'Ver demandas'-style navigation affordances on /painel itself; the entry-point link on / uses the existing secondary/outline treatment ('Limpar filtros' style), never bg-blue-700."
    - "MUST NOT alter demanda-list.tsx's existing 'Nova demanda' button, header count badge, or any other already-shipped element in that header row beyond adding the new conditional link alongside it."
  artifacts:
    - "src/app/(dashboard)/page.tsx — passes a new isCoordenador boolean prop (derived from the already-read role value) down to DemandaList"
    - "src/app/(dashboard)/demandas/demanda-list.tsx — renders the 'Painel do coordenador' link in its header row when isCoordenador is true, alongside the existing 'Nova demanda' button"
  key_links:
    - "page.tsx's existing profiles.role read (already used for the role-scoped-view notice since Phase 5) -> a new isCoordenador prop -> demanda-list.tsx's header row -> a real, working /painel destination (plan 06-01) — the complete, minimal wiring for COORD-01's 'one dashboard' to actually be discoverable from the app's main screen."
---

<objective>
**Phase goal (per ROADMAP.md):** Coordenador can view, in a single dashboard, the real status of every demanda, project, and volunteer across the institution.

Plan 06-01 built `/painel` itself; this plan (Wave 2, depends on 06-01 existing) adds the one entry point 06-UI-SPEC.md specifies: a single "Painel do coordenador" text link on `/`'s existing header row, visible only to `coordenador_geral`. This is deliberately sequenced after 06-01 so the link never points to a not-yet-built destination mid-phase.

Purpose: make the new dashboard actually discoverable — without this plan, a coordenador would have to know or be told the `/painel` URL directly, which defeats COORD-01's "single dashboard" framing as something the coordenador can actually find and use day to day.
Output: `/`'s header row shows a "Painel do coordenador" link for `coordenador_geral` only, next to the existing "Nova demanda" button; every other role sees the header row completely unchanged from Phase 4/5.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-coordinator-overview-dashboard/06-UI-SPEC.md
@.planning/phases/06-coordinator-overview-dashboard/06-01-SUMMARY.md
@src/app/(dashboard)/page.tsx
@src/app/(dashboard)/demandas/demanda-list.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Coordenador-only "Painel do coordenador" entry-point link on /</name>
  <precondition>Plan 06-01 is complete: `src/app/(dashboard)/painel/page.tsx` exists and `/painel` is a working route.</precondition>
  <read_first>
    - .planning/phases/06-coordinator-overview-dashboard/06-UI-SPEC.md ("Entry Point (nav link into /painel)" section in full — exact copy, exact styling reuse of the "Limpar filtros" desktop treatment, exact placement rationale)
    - src/app/(dashboard)/page.tsx (the exact current profile/role read — this task threads its existing `role` value into a new prop, it does not add a new query)
    - src/app/(dashboard)/demandas/demanda-list.tsx (the exact header row markup being extended — the `flex ... justify-between` block containing the `Nova demanda` `<Link>` and the count badge)
    - src/app/(dashboard)/demandas/demanda-filters.tsx (read only the `Limpar filtros` desktop button's exact className string — lines with `border-zinc-400 bg-white text-zinc-900` / the `lg:border-none lg:bg-transparent lg:underline` variant — this plan's new link reuses that treatment, not a new one)
  </read_first>
  <files>src/app/(dashboard)/page.tsx, src/app/(dashboard)/demandas/demanda-list.tsx</files>
  <action>
    **1. Extend `demanda-list.tsx`'s `DemandaListProps`** with a new optional prop `isCoordenador?: boolean` (default `false`). In the existing header row (`flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between`, the block currently containing the title/count on the left and the `Nova demanda` `<Link>` on the right), when `isCoordenador` is true, render a second link immediately adjacent to `Nova demanda`: `<Link href="/painel">Painel do coordenador</Link>`, styled as a secondary/outline button — reuse the exact `border border-zinc-400 bg-white text-zinc-900` treatment (matching `demanda-filters.tsx`'s `Limpar filtros` desktop/mobile classes: `min-h-14`, hover `hover:bg-zinc-100`, focus-visible outline ring identical to every other interactive element in this codebase). Both buttons sit in the same `flex-col sm:flex-row` wrapper the existing header row already uses, so on mobile they stack full-width vertically and on `sm:`+ they sit side by side — no new responsive logic needed, only reusing the wrapper's existing behavior with a second child. When `isCoordenador` is false or absent, render nothing new — the header row is byte-identical to its current Phase 4/5 output.

    **2. Extend `page.tsx`** to compute `const isCoordenador = role === "coordenador_geral";` immediately after the existing `role` read (no new Supabase query — `role` is already fetched from the existing `profiles` select alongside `email`), and pass `isCoordenador={isCoordenador}` to `<DemandaList ... />` alongside the existing `demandas`/`groupBy`/`filtersActive` props.

    Do not touch the existing `scopedViewNotice` logic, the existing área/responsável filter/query logic, or any other part of either file beyond this one additive prop and its rendering.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `npm test` exits 0 (zero regression to any existing suite).
    - `grep -q 'isCoordenador' src/app/\(dashboard\)/demandas/demanda-list.tsx` exits 0 and `grep -q 'isCoordenador' src/app/\(dashboard\)/page.tsx` exits 0.
    - `grep -q 'Painel do coordenador' src/app/\(dashboard\)/demandas/demanda-list.tsx` exits 0 (exact locked copy).
    - `grep -qE 'href="/painel"' src/app/\(dashboard\)/demandas/demanda-list.tsx` exits 0.
    - `grep -q 'coordenador_geral' src/app/\(dashboard\)/page.tsx` exits 0 (the prop is actually derived from the real role value, not hardcoded true).
    - `grep -c 'from("profiles")' src/app/\(dashboard\)/page.tsx` outputs 1 (no new/duplicate profile query added — the existing single read is reused).
    - `grep -q 'Nova demanda' src/app/\(dashboard\)/demandas/demanda-list.tsx` exits 0 (the pre-existing button is preserved, not replaced).
    - `grep -c 'bg-blue-700' src/app/\(dashboard\)/demandas/demanda-list.tsx` outputs 1 (unchanged from before this plan — only the pre-existing "Nova demanda" button uses the blue accent; the new coordenador link does not add a second occurrence).
  </acceptance_criteria>
  <done>A coordenador_geral visiting / sees a "Painel do coordenador" link next to "Nova demanda" in the header row, styled identically to the existing "Limpar filtros" secondary treatment, linking to the working /painel route from plan 06-01; every other role's header row is rendered exactly as it was before this plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `page.tsx`'s existing `profiles.role` read → `demanda-list.tsx`'s link visibility | Purely a UX-hiding decision, same as every other role-conditional render already in this codebase (the `scopedViewNotice` logic) — RLS on `/painel`'s own queries (plan 06-01) remains the actual authorization boundary regardless of whether this link is ever shown to the wrong role by a future bug |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05 | Information Disclosure | A non-coordenador somehow sees the "Painel do coordenador" link (e.g. a future refactor drops the `isCoordenador` check) and clicks it | low | accept | Even in that scenario, `/painel`'s own role branch (plan 06-01) still renders the calm access-denied state and no institution-wide data is ever queried for that role — this link's visibility is UX-only, never a security control, mirroring every other hide-vs-authorize distinction already established in this codebase |
| T-06-SC | Tampering | Supply chain via package installs | low | accept | This plan installs zero new packages — pure JSX/prop-threading over already-audited components |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npm test` green.
- Manual: as `coordenador_geral`, visit `/` and confirm the "Painel do coordenador" link appears next to "Nova demanda", is `min-h-14`, and navigates to a working `/painel`.
- Manual: as `lider_area`/`voluntario_comum`/`financeiro`, visit `/` and confirm the header row is unchanged from before this plan (no new link, no layout shift).
- Manual: at a narrow mobile width, confirm both buttons stack full-width without crowding (06-UI-SPEC.md Open Decision #2's flagged visual check).
</verification>

<success_criteria>
COORD-01's "single dashboard" is genuinely discoverable: a coordenador has one clear, always-visible path from their everyday `/` screen into `/painel`, with zero visibility for any other role and zero new queries or dependencies.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 06-02 contribution)

**Modified file paths**
- `src/app/(dashboard)/page.tsx` — threads the existing `role` read into a new `isCoordenador` prop
- `src/app/(dashboard)/demandas/demanda-list.tsx` — renders the coordenador-only entry-point link in its header row

**Phase 6 complete after this plan** — COORD-01/02/03 are fully satisfied: `/painel` exists and works (plan 06-01), and it is discoverable from the app's main screen (this plan).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/06-coordinator-overview-dashboard/06-02-SUMMARY.md` when done.
</output>
