---
phase: 08-ai-task-extraction-review
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified: []
autonomous: false
requirements: [IA-01, IA-03, IA-04]
user_setup:
  - service: google-ai-studio
    why: "GEMINI_API_KEY does not exist anywhere in this project yet (confirmed by 08-RESEARCH.md's Runtime State Inventory — no GEMINI/GOOGLE_GENAI reference found anywhere under src/ before plan 08-01). It has no code-level fallback: extractDemandas cannot function without it, in local dev or in Vercel Production. A human must obtain it and add it in both places; no agent can generate or supply a real API key."
    env_vars:
      - name: GEMINI_API_KEY
        source: "Google AI Studio (ai.google.dev/gemini-api) -> Get API key. A free Google account is sufficient — no billing is required to obtain a Gemini API free-tier key. Add to .env.local for local dev, and to Vercel Production via `vercel env add GEMINI_API_KEY production`."
    dashboard_config:
      - task: "Confirm the current, non-deprecated Gemini model name one more time directly in Google AI Studio's model picker (or ai.google.dev/gemini-api/docs/models) immediately before this plan's live verification step — 08-RESEARCH.md flagged its own model-name research as LOW confidence with a 14-day shelf life specifically because Gemini's model line was observed changing mid-research; plan 08-01's model-name choice should be re-confirmed still current at this point, not re-assumed."
      - task: "Confirm GEMINI_API_KEY is present and non-empty in Vercel Production"
        location: "Vercel Dashboard -> Project -> Settings -> Environment Variables (Production), or `vercel env ls production`"

must_haves:
  truths:
    - "GEMINI_API_KEY exists in Vercel Production before this plan is considered complete — `vercel env ls production` lists it by name (value never inspected or echoed by any automated step, per this project's established secret-handling discipline from Phase 1/7's own equivalent checkpoints)."
    - "The deployed /demandas/extrair flow works end-to-end against the REAL Gemini API in production — not merely 'the code compiles' or 'the mocked tests pass' — proven by a human pasting a real (fabricated-for-testing, non-sensitive) meeting summary and observing actual suggested demandas returned, reviewed, and confirmed into the real production database via the unchanged createDemanda path (LEMB-04-equivalent observability requirement extended to this phase: success/failure must be genuinely observable, not assumed)."
    - "The Gemini model name used in production is confirmed still current/non-deprecated at verification time — this closes 08-RESEARCH.md's own explicitly-flagged 14-day-shelf-life gap on the model-name research, rather than silently trusting a value chosen during Wave 1's execution."
    - "A confirmed suggestion, once created via createDemanda in production, appears in the ordinary /demandas list exactly like any manually-created demanda — indistinguishable in storage and RLS treatment, proving IA-04's invariant held through to a real production write."
  prohibitions:
    - "MUST NOT add GEMINI_API_KEY to any file this plan creates or edits — this is a Vercel Production secret entered directly by a human via `vercel env add` or the Dashboard UI; no command-line argument, committed file, or automated step in this plan ever holds or echoes a real value."
    - "MUST NOT add a new supabase/migrations/*.sql file — this plan is a pure deployment-and-verification wave, zero schema change, matching Phase 7's own 07-03 zero-new-migration precedent for its equivalent closing wave."
    - "MUST NOT resolve a failed production verification step by editing code directly during this checkpoint or by skipping the checkpoint — any real defect found during this plan's human verification routes back to a new plan/gap-closure cycle, per this project's established Phase 2 (02-04) precedent for phase-gate checkpoints."
    - "MUST NOT paste any real institutional meeting content, real volunteer names, or real personal email addresses into the production verification test — use clearly fabricated, obviously-fictional test content (e.g. invented names, invented tasks) so a stray persisted test demanda is unambiguously identifiable as test data, not real institutional data."
  artifacts:
    - ".planning/phases/08-ai-task-extraction-review/08-03-SUMMARY.md — records the observed outcome of the production verification, including the actual Gemini model name used, its confirmed-current status at verification time, and whether any real defects were found"
  key_links:
    - "GEMINI_API_KEY's presence in Vercel Production <-> the deployed extractDemandas Server Action's process.env.GEMINI_API_KEY read (plan 08-01) — this exact chain has never been exercised against the real Gemini API before this plan's verification step, since Wave 1's own tests mock @google/genai entirely and local dev may not have had a real key either."
    - "The verified-current Gemini model name <-> plan 08-01's hardcoded model constant — if this plan's re-verification finds the model name plan 08-01 chose has ALREADY been deprecated/restricted in the time since (a real, documented risk given 08-RESEARCH.md's own 14-day shelf-life flag), this plan's finding routes to a small follow-up fix (a one-line model-name string change per 08-RESEARCH.md's own framing of this risk as small-blast-radius), not a re-architecture."
---

<objective>
**Phase goal (per ROADMAP.md):** Users paste a meeting summary already generated by an external tool (Fireflies/tl;dv), and new demandas are proposed from it by AI — but only created after a human confirms them, never automatically.

This plan is Wave 3 — the phase's closing wave, doing the one thing no earlier wave could: putting the deployed extraction flow to work against the real Gemini API in production, and confirming end-to-end that IA-01/IA-03/IA-04 all hold true outside of mocked tests. It has two parts, in this order. First, a `checkpoint:human-action` task: `GEMINI_API_KEY` has never existed as an environment variable anywhere in this project — not locally, not in Vercel Production — and it has no code-level fallback. This genuinely requires a human to obtain it from Google AI Studio and add it in both places; no agent can generate or supply a real API key. Second, once the key is present and the app is deployed, a verification task walks through the real, live flow once: paste a fabricated test meeting summary, observe real Gemini-extracted suggestions, review/edit/reject/confirm them, and confirm a resulting demanda lands correctly in the production database via the unmodified `createDemanda` path.

Purpose: close the one deployment gap this phase's own research explicitly flagged as a required, non-optional new step (not a silent assumption folded into "deploy"), and prove — against the real external API, not a mock — that the phase's core invariant (IA-04: never created without review) survives contact with a genuinely non-deterministic AI response.
Output: `GEMINI_API_KEY` live in Vercel Production; the `/demandas/extrair` flow verified working end-to-end against the real Gemini API, with a real confirmed demanda proven to land correctly in production.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ai-task-extraction-review/08-RESEARCH.md
@.planning/phases/08-ai-task-extraction-review/08-01-SUMMARY.md
@.planning/phases/08-ai-task-extraction-review/08-02-SUMMARY.md
@.planning/phases/07-email-reminders/07-03-PLAN.md
@.planning/phases/01-project-scaffold-institutional-login/01-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Add GEMINI_API_KEY to Vercel Production, then verify the deployed /demandas/extrair flow against the real Gemini API</name>
  <read_first>
    - .planning/phases/08-ai-task-extraction-review/08-RESEARCH.md ("Runtime State Inventory" lines 388-400, "Environment Availability" lines 600-612, "Open Question 1" lines 590-598)
    - .planning/phases/01-project-scaffold-institutional-login/01-04-SUMMARY.md (the exact precedent for `vercel env add`/`vercel env ls production` command shapes already used successfully in this project)
    - .planning/phases/07-email-reminders/07-03-PLAN.md (Task 1 — the exact structural template this checkpoint follows: a `checkpoint:human-action` gate for a brand-new required secret with no code-level fallback)
  </read_first>
  <action>Enter a real Gemini API key into Vercel Production yourself — `vercel env add` prompts interactively for the value and no agent can supply a real GEMINI_API_KEY on your behalf. Then redeploy, and run the live end-to-end verification below using the real, deployed application.</action>
  <instructions>
    I already built and locally type-checked the full paste-and-review flow (plans 08-01/08-02) — a working `extractDemandas` Server Action that calls Gemini with `responseSchema` enforcement, a responsável-matching heuristic, and a review screen whose "Confirmar" reuses the existing `createDemanda` action unchanged. Every piece of this has been proven correct against MOCKED Gemini responses in automated tests. None of it has ever been exercised against the REAL Gemini API, because `GEMINI_API_KEY` has never existed as an environment variable anywhere in this project — not in `.env.local`, not in Vercel Production. I cannot add it myself — it requires a value only you can obtain.

    Please do the following, from the repository root:

    1. Go to Google AI Studio (`ai.google.dev/gemini-api` -> "Get API key") and generate a Gemini API key. A free Google account is sufficient; no billing setup is required to get a free-tier key.
    2. While you're there, open the model picker (or `ai.google.dev/gemini-api/docs/models`) and confirm which Flash-Lite-class (or similarly low-cost/low-latency) model name is CURRENTLY live and non-deprecated on your account. 08-RESEARCH.md flagged its own model-name recommendation as having an unusually short (14-day) shelf life because Gemini's model line was observed changing mid-research — tell me what model name plan 08-01 actually used (check `src/app/(dashboard)/demandas/extrair/actions.ts`) and confirm whether that name is still current, or whether it's already been superseded/deprecated since implementation.
    3. Add the key locally first: add `GEMINI_API_KEY=<your key>` to `.env.local` (not committed — this file is already gitignored) and confirm the flow works against `npm run dev` locally before touching production, if you'd like an extra safety check.
    4. `vercel env add GEMINI_API_KEY production` — paste the key value when prompted.
    5. Run `vercel env ls production` and confirm `GEMINI_API_KEY` appears (values are never shown by this command — that's expected).
    6. `vercel --prod` to redeploy so the new environment variable is actually available to the deployed function.
    7. Visit the deployed production URL, sign in as a coordenador_geral or líder de área account, and navigate to `/demandas/extrair` (via the "Extrair demandas de reunião" link on the home page, or directly).
    8. Paste a clearly FABRICATED test meeting summary — invented names, invented tasks, nothing resembling real institutional data (e.g. "Na reunião de hoje, Ana ficou responsável por revisar o relatório até sexta-feira. Bruno vai atualizar a planilha de doações até o fim do mês."). Click "Processar."
    9. Confirm the "Analisando..." pending state appears, then confirm real suggested demandas appear (título/responsável/prazo fields, pre-filled from the actual Gemini response — not a mock). Report what Gemini actually returned (the suggestion count and roughly what fields it extracted), so I can sanity-check the extraction quality.
    10. On the review screen: edit one suggestion's título, confirm one suggestion (click "Confirmar"), reject a second one (click "Rejeitar"), and — if a third exists — leave it pending momentarily to confirm "Concluir revisão" stays disabled until everything is resolved, then resolve it too.
    11. Confirm the "Confirmar" you clicked actually created a real row: check Supabase Dashboard's Table Editor -> `public.demandas` for a new row matching your edited título, and confirm it also appears in the ordinary `/` demandas list in the app.
    12. Report back: did every step behave as expected? Specifically flag anything that deviated — a wrong/garbled extraction, a responsável that should have matched but showed "não identificado" (or vice versa — a wrong auto-match), a Confirmar that failed, or any error message you didn't expect.
    13. (Optional cleanup) If you'd like the fabricated test demanda removed from production afterward, either mark it concluída or delete it via Supabase Table Editor — your call; it's a real database row now, indistinguishable from any other demanda.

    Do not paste your real GEMINI_API_KEY value into your reply — confirm presence/success/failure only.
  </instructions>
  <verification>After you confirm, I will re-check `vercel env ls production` output you report (names only), and I can independently confirm via the Supabase Table Editor query pattern already used elsewhere in this project that a new `demandas` row exists matching the título you reported editing/confirming.</verification>
  <resume-signal>Type "approved" if all thirteen steps behaved as expected (including reporting the actual Gemini model name and its confirmed-current status), or describe exactly which step deviated and what you saw (error message, wrong extraction, unexpected match/no-match, or a failed Confirmar).</resume-signal>
  <done>GEMINI_API_KEY is confirmed present in Vercel Production; the deployed /demandas/extrair flow has been exercised once against the real Gemini API with a fabricated test summary, producing real suggestions that were edited, confirmed, and rejected through the UI; a resulting demanda is confirmed to exist in the production database via the unmodified createDemanda path; and the Gemini model name in use has been re-confirmed current/non-deprecated at verification time, closing 08-RESEARCH.md's own flagged model-name shelf-life gap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human operator -> Vercel Production secret store | The first and only place `GEMINI_API_KEY` is ever entered for the deployed environment; no automated step in this plan holds, generates, or transmits the value |
| Deployed production `/demandas/extrair` -> the real Gemini API | Now genuinely reachable with real credentials for the first time — every prior test exercised this boundary only via a mock |
| The fabricated test meeting summary -> the real Gemini API | The first genuinely untrusted, uncontrolled text this flow has ever sent to a live external model — Task 1's step 8 deliberately uses fully fictional content so no real institutional/personal data is ever transmitted to Google during this verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-09 | Information Disclosure | A real GEMINI_API_KEY value appearing in this plan's SUMMARY, in shell history, or in a committed file | critical | mitigate | Task 1's acceptance criteria explicitly require presence/success-only reporting; `vercel env ls production` never displays values by design; the resume-signal instructs against pasting the real key |
| T-08-10 | Information Disclosure | Real institutional meeting content or real volunteer names being pasted into the live verification, then transmitted to Google's Gemini API as part of this checkpoint | medium | mitigate | Task 1's instructions explicitly require a clearly fabricated, fictional test summary — invented names, invented tasks — and the must_haves prohibitions state this explicitly, not merely as a suggestion |
| T-08-11 | Tampering (Supply Chain) | The Gemini model name in use having already been deprecated/restricted between plan 08-01's implementation and this plan's production verification | medium | mitigate | Task 1 step 2 explicitly re-checks the model's current status live in Google AI Studio before/during verification — this is the specific check that catches 08-RESEARCH.md's own flagged 14-day-shelf-life risk materializing |
| T-08-12 | Denial of Service | Gemini API rate limits or free-tier quota being insufficient at even this single verification call, given 08-RESEARCH.md's own LOW-confidence framing of current free-tier limits | low | accept | A single manual verification call is far below any plausible rate limit even under the most conservative third-party estimate 08-RESEARCH.md found; if quota IS insufficient, the error surfaces clearly during this checkpoint rather than silently failing later |
</threat_model>

<verification>
- `vercel env ls production` lists `GEMINI_API_KEY`.
- A real paste-to-suggestions-to-confirm cycle succeeds against the deployed production app, using the real Gemini API, with the resulting demanda visible in Supabase Table Editor and the ordinary `/` demandas list.
- The Gemini model name in use is confirmed current/non-deprecated at verification time by a live check in Google AI Studio or the official docs, not assumed from plan 08-01's implementation-time choice.
</verification>

<success_criteria>
The full Phase 8 goal is achieved end-to-end, against the real world, not just mocked tests: a human can paste a ready-made meeting summary into the deployed production app, see AI-suggested demandas with título/responsável/prazo, edit or reject each one individually, and confirm that only the explicitly-confirmed suggestions become real demandas — proven by a real row landing in the production database through the exact same, unmodified createDemanda path every other demanda in this system has ever gone through.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 08-03 contribution)

**New file paths**
- `.planning/phases/08-ai-task-extraction-review/08-03-SUMMARY.md`

**Deployment state changes**
- Vercel Production environment variables: `GEMINI_API_KEY` (new)

**Findings carried forward**
- The observed real Gemini extraction quality (suggestion count, título/responsável/prazo accuracy) and the confirmed-current Gemini model name at verification time, recorded in the SUMMARY for future reference if extraction quality or model deprecation ever needs revisiting.
- Whether the Confirmation Feedback landing banner (explicitly deferred in plan 08-02's SUMMARY) is worth a follow-up gap-closure plan, based on how disorienting its absence actually felt during this real walkthrough.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/08-ai-task-extraction-review/08-03-SUMMARY.md` when done.
</output>
