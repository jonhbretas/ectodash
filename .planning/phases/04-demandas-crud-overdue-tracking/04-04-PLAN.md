---
phase: 04-demandas-crud-overdue-tracking
plan: 04
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - src/app/(dashboard)/demandas/status-badge.tsx
  - src/app/(dashboard)/demandas/overdue-badge.tsx
  - src/app/(dashboard)/demandas/demanda-card.tsx
  - src/app/(dashboard)/demandas/demanda-table.tsx
  - src/app/(dashboard)/demandas/demanda-list.tsx
  - src/app/(dashboard)/page.tsx
autonomous: true
requirements: [DEM-03, DEM-01]
must_haves:
  truths:
    - "A demanda whose prazo has passed and whose status is not concluída is visually flagged as atrasada automatically, with no manual marking step anywhere in the UI (DEM-03)."
    - "The atrasada flag and every status are conveyed through icon + color + always-visible text label together — never color alone, satisfying 04-UI-SPEC.md's explicit non-color-alone rule for both colorblind and low-vision elderly users."
    - "A concluded demanda is never shown as atrasada on screen, regardless of how far in the past its prazo is — matching the same rule already proven at the data layer in plan 04-01 (04-UI-SPEC.md Overdue Visual Treatment)."
    - "The demandas list renders as full-width stacked cards below the lg breakpoint and as a table at lg and above, with the exact column set and sort order 04-UI-SPEC.md specifies (atrasada first, then prazo ascending, then concluída last) (04-UI-SPEC.md Screen Inventory section 1, UX-03/responsive requirement carried forward from Phase 1's floor)."
    - "Zero demandas renders the documented empty-state heading, body, and inline Nova demanda CTA — never a bare blank page (04-UI-SPEC.md UI Considerations, empty row)."
    - "A demanda with no área/projeto set renders the neutral Sem área definida placeholder chip instead of a blank cell (04-UI-SPEC.md UI Considerations, empty backstop row)."
  prohibitions:
    - "MUST NOT introduce shadcn/ui — same locked decision as plan 04-02 (04-UI-SPEC.md, deferred to Phase 3)."
    - "MUST NOT compute the atrasada boolean in this plan's rendering code from a client-side date comparison — it must be read directly from demandas_com_status's already-computed atrasada column (plan 04-01), never re-derived client-side (RESEARCH.md Pattern 2, Anti-Patterns: never trust a client-computed value as the source of truth for whether something is overdue)."
    - "MUST NOT convey atrasada or any status by background color alone anywhere in the new components — every instance pairs an icon and a text label with the color, per 04-UI-SPEC.md's explicit rule."
    - "MUST NOT add a delete action, a role-based visibility filter, or a coordinator-specific aggregate view in this plan — deletion is out of scope this phase, role-scoping is Phase 5, and the coordinator dashboard is Phase 6."
    - "MUST NOT build a custom date-picker or combobox component here — this plan is list/read-side rendering only, it does not touch the create/edit form."
  artifacts:
    - "src/app/(dashboard)/demandas/status-badge.tsx — reusable badge for pendente/em_andamento/concluída, icon+color+label per 04-UI-SPEC.md's Status Representation table"
    - "src/app/(dashboard)/demandas/overdue-badge.tsx — the Atrasada badge with aria-label announcing the prazo date, per 04-UI-SPEC.md's accessible-name requirement"
    - "src/app/(dashboard)/demandas/demanda-card.tsx — extended (from plan 04-02's minimal version) to the full mobile card layout: título, status badge, responsável row, prazo row with overdue treatment, área chip"
    - "src/app/(dashboard)/demandas/demanda-table.tsx — the desktop table view with the same data, including the border-l-4 overdue row stripe"
    - "src/app/(dashboard)/demandas/demanda-list.tsx — the breakpoint-switching container (cards below lg, table at lg+), sort order, empty state"
  key_links:
    - "demandas_com_status.atrasada (plan 04-01, computed server-side) -> demanda-list.tsx's sort -> demanda-card.tsx / demanda-table.tsx's OverdueBadge — the value is read once from the database and threaded through unchanged; no component in this chain recomputes it."
    - "page.tsx's existing query (plan 04-02) -> demanda-list.tsx — this plan replaces plan 04-02's minimal inline rendering in page.tsx with the full DemandaList component, without changing the underlying demandas_com_status query shape."
---

<objective>
**Phase goal (per ROADMAP.md):** Users can create, edit, and track the status of demandas (tasks), with overdue items flagged automatically without manual marking.

This plan delivers DEM-03's visual contract in full and completes DEM-01's list-rendering half: the responsive card/table layout, the non-color-alone status and overdue badges, correct sort ordering, and the empty/missing-área backstop states — all exactly as locked in `04-UI-SPEC.md`.

It runs in the same wave as plan 04-03 (edit/conclude) and touches a disjoint set of files — this plan owns list-rendering presentational components and `page.tsx`'s render output; plan 04-03 owns the edit route and mutation actions. Both depend only on plan 04-02's tracer.

Purpose: turn the tracer's minimal list (plan 04-02) into the actual locked visual contract, and prove DEM-03's "automatic, no manual marking" claim is not just true in the database (plan 04-01) but visibly true on screen.
Output: the demandas list matches 04-UI-SPEC.md's Screen Inventory section 1 exactly, on both mobile and desktop.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-demandas-crud-overdue-tracking/04-CONTEXT.md
@.planning/phases/04-demandas-crud-overdue-tracking/04-RESEARCH.md
@.planning/phases/04-demandas-crud-overdue-tracking/04-UI-SPEC.md
@.planning/phases/04-demandas-crud-overdue-tracking/04-01-SUMMARY.md
@.planning/phases/04-demandas-crud-overdue-tracking/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Status and overdue badges — the non-color-alone building blocks</name>
  <precondition>Plan 04-02's demandas_com_status read in page.tsx exists and returns an atrasada boolean per row (04-02-SUMMARY.md confirms this).</precondition>
  <read_first>
    - .planning/phases/04-demandas-crud-overdue-tracking/04-UI-SPEC.md (sections "Status Representation" and "Overdue (\"Atrasada\") Visual Treatment" in full — every icon name, color, and copy string is locked exactly as written)
    - .planning/phases/04-demandas-crud-overdue-tracking/04-UI-SPEC.md (section "Color" — the amber/blue/green/red role table, and the explicit "non-color-alone rule")
  </read_first>
  <files>src/app/(dashboard)/demandas/status-badge.tsx, src/app/(dashboard)/demandas/overdue-badge.tsx</files>
  <action>
    **1. `status-badge.tsx`** — a small presentational component, `<StatusBadge status="pendente" | "em_andamento" | "concluida">`. Render a `rounded-full px-2 py-0.5 text-base font-semibold` pill containing an icon (from `lucide-react`, installed in plan 04-02) and the Portuguese label, per 04-UI-SPEC.md's table exactly: `pendente` → `Circle` icon (outline), `amber-700` text on `amber-100` background, `border border-amber-300`, label `Pendente`; `em_andamento` → `Clock` icon, `blue-700` on `blue-100`, `border border-blue-300`, label `Em andamento`; `concluida` → `CheckCircle2` icon (filled), `green-700` on `green-100`, `border border-green-300`, label `Concluída`. Icon and label are both always rendered together — never the icon alone, never the color alone.

    **2. `overdue-badge.tsx`** — `<OverdueBadge prazo={dateString}>`, rendered only by the caller when `atrasada === true` (this component itself does not receive or check the boolean — the caller already filtered on the value read from `demandas_com_status`, keeping this component a pure rendering concern with the decision made once, upstream). Render the same pill shape: `AlertTriangle` icon (16-20px, `lucide-react`), `red-700` text/icon, `bg-red-100 text-red-800 border border-red-300`, label text `Atrasada` always visible (never the badge shown as color/icon alone). Format `prazo` to `dd/MM/yyyy` via `date-fns`'s `format` with the `ptBR` locale for the `aria-label`: `aria-label={`Atrasada — prazo era ${formatted}`}` exactly matching 04-UI-SPEC.md's accessible-name spec.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `grep -q 'Pendente' src/app/\(dashboard\)/demandas/status-badge.tsx` exits 0 and `grep -q 'Em andamento' src/app/\(dashboard\)/demandas/status-badge.tsx` exits 0 and `grep -q 'Concluída' src/app/\(dashboard\)/demandas/status-badge.tsx` exits 0.
    - `grep -q 'amber-700' src/app/\(dashboard\)/demandas/status-badge.tsx` exits 0 and `grep -q 'blue-700' src/app/\(dashboard\)/demandas/status-badge.tsx` exits 0 and `grep -q 'green-700' src/app/\(dashboard\)/demandas/status-badge.tsx` exits 0.
    - `grep -qE 'Circle|Clock|CheckCircle2' src/app/\(dashboard\)/demandas/status-badge.tsx` exits 0 (icons from lucide-react are present, not color-only).
    - `grep -q 'Atrasada' src/app/\(dashboard\)/demandas/overdue-badge.tsx` exits 0.
    - `grep -q 'AlertTriangle' src/app/\(dashboard\)/demandas/overdue-badge.tsx` exits 0.
    - `grep -q 'aria-label' src/app/\(dashboard\)/demandas/overdue-badge.tsx` exits 0.
    - `grep -qE "prazo era" src/app/\(dashboard\)/demandas/overdue-badge.tsx` exits 0 (exact accessible-name copy from 04-UI-SPEC.md).
    - `grep -q 'ptBR' src/app/\(dashboard\)/demandas/overdue-badge.tsx` exits 0 (pt-BR date formatting, not a raw ISO string, per the copywriting contract).
    - `grep -qE 'isPast|new Date\(\) *[<>]' src/app/\(dashboard\)/demandas/overdue-badge.tsx` ; test $? -eq 1 (no client-side "is this overdue" recomputation inside the badge component — it only formats a date it's given, per the Anti-Patterns rule).
  </acceptance_criteria>
  <done>StatusBadge renders all 3 statuses and OverdueBadge renders the Atrasada indicator, each pairing icon + color + always-visible text label exactly per 04-UI-SPEC.md, with the overdue badge's screen-reader text stating the actual prazo date.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Responsive demandas list — cards, table, sort order, empty state, and área backstop</name>
  <precondition>Task 1's StatusBadge and OverdueBadge exist.</precondition>
  <read_first>
    - .planning/phases/04-demandas-crud-overdue-tracking/04-UI-SPEC.md (section "Screen Inventory -> 1. Demandas list" in full — header, mobile card layout rows 1-4, desktop table columns, sort order, tablet behavior)
    - .planning/phases/04-demandas-crud-overdue-tracking/04-UI-SPEC.md (section "Screen Inventory -> 2. Empty state" in full — exact heading/body/CTA copy)
    - .planning/phases/04-demandas-crud-overdue-tracking/04-UI-SPEC.md (section "UI Considerations" table, rows for "empty | Área/projeto field" and "overflow | Demanda título" — the Sem área definida backstop and line-clamp truncation)
    - src/app/(dashboard)/demandas/demanda-card.tsx (plan 04-02's minimal version — extend it, do not replace its file identity)
    - src/app/(dashboard)/page.tsx (plan 04-02's current list-rendering code — this task replaces its inline rendering with DemandaList, keeping the same demandas_com_status query)
  </read_first>
  <files>src/app/(dashboard)/demandas/demanda-card.tsx, src/app/(dashboard)/demandas/demanda-table.tsx, src/app/(dashboard)/demandas/demanda-list.tsx, src/app/(dashboard)/page.tsx</files>
  <action>
    **1. Extend `demanda-card.tsx`** to the full mobile layout per 04-UI-SPEC.md: a full-width card, `p-4`, `bg-white border border-zinc-300 rounded-lg`, wrapped entirely in a `<Link href={`/demandas/${id}/editar`}>` (the whole card is the tap target, per the spec — no separate "Editar" button). Row 1: título (`text-lg font-semibold line-clamp-2`) with `<StatusBadge>` top-right. Row 2: responsável name(s) — for multiple responsáveis, join with `, ` and if the joined string would be long, let it wrap naturally (no truncation rule is specified for this row beyond the card's own width) — prefixed with a `User` icon (`lucide-react`, 16px). Row 3: prazo formatted `dd/MM/yyyy` via `date-fns`/`ptBR`, prefixed with a `Calendar` icon (16px); when `atrasada` is true, replace this row's icon/text color with `red-700` and additionally render `<OverdueBadge>` inline. Row 4 (only if área is set): a neutral chip `bg-zinc-100 text-zinc-700`; when área is not set, render the `Sem área definida` placeholder chip `bg-zinc-100 text-zinc-500` instead of omitting the row — per the UI Considerations backstop row, this must never simply be blank.

    **2. `demanda-table.tsx`** — the desktop (`lg:` and above) table view. Standard `<table>`: header row `bg-zinc-100 text-zinc-700 text-base font-semibold`, columns Título | Responsável | Prazo | Status | Área/Projeto. Body rows `text-xl`, `py-3` cell padding, each row wrapped so a click anywhere in the `<tr>` navigates to `/demandas/${id}/editar` (`cursor-pointer`, `hover:bg-zinc-50`). Título cell: 1-line truncation with ellipsis (not `line-clamp-2` — that's the card treatment) and a native `title` attribute holding the full text for a hover/focus tooltip. Overdue rows: `border-l-4 border-red-700` left-edge stripe, in addition to the `<OverdueBadge>` inline in the Prazo cell (04-UI-SPEC.md's explicit "redundant but scannable second signal" for a full table). Área cell: same `Sem área definida` backstop as the card.

    **3. `demanda-list.tsx`** — the container component receiving the already-fetched array of demandas (each already carrying its resolved responsável names and its `atrasada` boolean from the caller). Responsibilities:
    - **Sort**: atrasada items first, then by `prazo` ascending among the rest, then `concluida` items last regardless of prazo — implement as a single comparator, not three separate array operations, so the ordering rule stays legible in one place. (Per 04-UI-SPEC.md's Sort order note.)
    - **Breakpoint switch**: render `<DemandaCard>` list (wrapped `gap-4` vertical stack) unconditionally in markup, and `<DemandaTable>` also in markup, using Tailwind's `lg:hidden` / `hidden lg:block` pair on their respective wrapping elements — this is the single `lg:` breakpoint switch 04-UI-SPEC.md specifies (cards below `lg`, including tablet; table at `lg` and above). Do not implement the switch via JS/`useEffect`/`window.innerWidth` — CSS-only, so it works correctly on first paint with no layout flash.
    - **Empty state**: when the array is empty, render 04-UI-SPEC.md's exact empty-state block instead of either list variant — `ClipboardList` icon (48px, `text-zinc-400`), heading `Nenhuma demanda cadastrada ainda` (`text-3xl font-semibold`), body `Quando alguém criar uma demanda, ela vai aparecer aqui. Toque em "Nova demanda" para começar.` (`text-xl text-zinc-700 max-w-md`), and an inline `Nova demanda` CTA button linking to `/demandas/nova`, `py-16` vertical padding, centered.
    - **Header**: `h1` `Demandas` (`text-2xl font-semibold`) plus a count badge (`{n} demandas` / `1 demanda` for the singular case — the one deliberate singular/plural copy variance per 04-UI-SPEC.md), and the `Nova demanda` primary CTA button (top-right on desktop, full-width below the header on mobile — a `flex-col sm:flex-row` header layout is sufficient for this).

    **4. `page.tsx`** — replace plan 04-02's inline minimal list-rendering with `<DemandaList demandas={...} />`, keeping the same `demandas_com_status` query and the same per-demanda responsável-resolution approach already established (optimize the N+1 from plan 04-02 into a single `.in("demanda_id", ids)` query grouped client-side if that wasn't already done — either is acceptable, but do not leave the query shape worse than plan 04-02 left it). Keep the existing `SignOutButton` and greeting; `DemandaList` replaces only the demandas-rendering portion of the page.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `npm test` exits 0.
    - `grep -q 'lg:hidden' src/app/\(dashboard\)/demandas/demanda-list.tsx` exits 0 and `grep -qE 'hidden lg:block|hidden lg:table' src/app/\(dashboard\)/demandas/demanda-list.tsx` exits 0 (CSS-only breakpoint switch, both variants present in markup).
    - `grep -c 'useEffect\|window.innerWidth\|matchMedia' src/app/\(dashboard\)/demandas/demanda-list.tsx` outputs 0 (no JS-based breakpoint detection).
    - `grep -q 'Nenhuma demanda cadastrada ainda' src/app/\(dashboard\)/demandas/demanda-list.tsx` exits 0 (exact empty-state copy).
    - `grep -q 'Sem área definida' src/app/\(dashboard\)/demandas/demanda-card.tsx` exits 0 and `grep -q 'Sem área definida' src/app/\(dashboard\)/demandas/demanda-table.tsx` exits 0 (backstop present in both layouts).
    - `grep -q 'line-clamp-2' src/app/\(dashboard\)/demandas/demanda-card.tsx` exits 0 (título truncation on the card).
    - `grep -q 'border-l-4' src/app/\(dashboard\)/demandas/demanda-table.tsx` exits 0 and `grep -q 'border-red-700' src/app/\(dashboard\)/demandas/demanda-table.tsx` exits 0 (the desktop-only overdue row stripe).
    - `grep -q 'StatusBadge' src/app/\(dashboard\)/demandas/demanda-card.tsx` exits 0 and `grep -q 'StatusBadge' src/app/\(dashboard\)/demandas/demanda-table.tsx` exits 0.
    - `grep -q 'OverdueBadge' src/app/\(dashboard\)/demandas/demanda-card.tsx` exits 0 and `grep -q 'OverdueBadge' src/app/\(dashboard\)/demandas/demanda-table.tsx` exits 0.
    - `grep -qE 'atrasada' src/app/\(dashboard\)/demandas/demanda-list.tsx` exits 0 (the sort comparator reads the server-computed atrasada field, not a recomputed one).
    - `grep -qE "isPast\(|new Date\(\) *[<>]" src/app/\(dashboard\)/demandas/demanda-list.tsx` ; test $? -eq 1 (no client-side overdue recomputation in the sort/list layer either).
    - `grep -q 'DemandaList' src/app/\(dashboard\)/page.tsx` exits 0.
    - `grep -q 'Nova demanda' src/app/\(dashboard\)/demandas/demanda-list.tsx` exits 0.
    - `grep -q 'SignOutButton' src/app/\(dashboard\)/page.tsx` exits 0 (Phase 1/2's sign-out control is preserved, not removed by this plan's edits).
  </acceptance_criteria>
  <done>The dashboard renders demandas as cards on mobile/tablet and a table on desktop, sorted atrasada-first then by prazo then concluída-last, with every overdue/status indicator shown via icon+color+label, área gaps backstopped with a placeholder chip, and an empty state matching 04-UI-SPEC.md exactly when there are zero demandas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| demandas_com_status.atrasada (server-computed) -> UI rendering | The UI must render, not recompute, this value — a rendering bug that re-derives it client-side could disagree with the database's own clock |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-14 | Tampering | A rendering bug that recomputes "is this overdue" client-side instead of trusting the server-provided `atrasada` boolean, disagreeing with the database under client clock skew | medium | mitigate | Grep-enforced in both this plan's tasks: no `isPast(`/manual `Date` comparison appears in `overdue-badge.tsx` or `demanda-list.tsx`; the badge and sort both consume the already-computed value from plan 04-01's view |
| T-04-15 | Information Disclosure | Status/overdue conveyed by color alone, silently excluding colorblind or low-vision users from information sighted users get for free | medium | mitigate | Every badge pairs an icon and an always-rendered text label with its color, grep-verified in Task 1's acceptance criteria; matches 04-UI-SPEC.md's explicit non-color-alone rule |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs zero new packages — reuses `lucide-react` and `date-fns` already audited `[OK]` and installed in plan 04-02 |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npm test` green.
- Manual: on a mobile viewport, demandas render as cards with all 4 documented rows; on a desktop viewport (≥1024px), the same data renders as a table with the overdue row stripe.
- Manual: a demanda with a past prazo and status ≠ concluída shows the Atrasada badge automatically, with no action taken to mark it; a demanda with the same past prazo but status = concluída does not show the badge.
- Manual: with zero demandas, the documented empty-state block renders instead of a blank list.
</verification>

<success_criteria>
DEM-03 is fully satisfied and visible on screen: every overdue demanda is automatically flagged with a redundant icon+color+text signal, never color alone, and the list itself renders correctly and responsively per 04-UI-SPEC.md's locked contract on both mobile and desktop.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 04-04 contribution)

**New file paths**
- `src/app/(dashboard)/demandas/status-badge.tsx`
- `src/app/(dashboard)/demandas/overdue-badge.tsx`
- `src/app/(dashboard)/demandas/demanda-table.tsx`
- `src/app/(dashboard)/demandas/demanda-list.tsx`

**Modified file paths**
- `src/app/(dashboard)/demandas/demanda-card.tsx` — extended from plan 04-02's minimal version to the full 04-UI-SPEC.md mobile card layout
- `src/app/(dashboard)/page.tsx` — inline list rendering replaced with `<DemandaList>`

**Contract for later phases**
- `StatusBadge` and `OverdueBadge` are the only components in the codebase rendering demanda status/overdue state — Phase 5's filtered views and Phase 6's coordinator dashboard reuse them rather than re-deriving the visual treatment.
- `DemandaList`'s sort comparator (atrasada first, then prazo ascending, then concluída last) is the phase's canonical demandas ordering — Phase 5's filtering extends this list's query, it does not replace the sort rule.
- This is the phase's first real UI surface Phase 3's later polish pass will lift tokens from wholesale, per 04-UI-SPEC.md's own "Sequencing Note."
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/04-demandas-crud-overdue-tracking/04-04-SUMMARY.md` when done.
</output>
